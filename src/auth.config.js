/**
 * ⚠️ SECURITY WARNING
 * These credentials are used for local demo purposes.
 * For production, never store passwords in plain text in the source code.
 * Consider using Supabase Auth for a more secure implementation.
 */
export const AUTH_CONFIG = {
    username: 'admin',
    password: 'changeme123'
};
